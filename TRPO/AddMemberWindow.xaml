<Window x:Class="TRPO.AddMemberWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TRPO"
        mc:Ignorable="d"
        Title="AddMemberWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Group.Title, StringFormat='Группа: {0}'}" 
                   FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,10">
            <Label Content="Пользователь:"/>
            <ComboBox ItemsSource="{Binding UserService.Users}"
                      SelectedItem="{Binding SelectedUser}"
                      DisplayMemberPath="Name"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical">
            <Label Content="Дата вступления:"/>
            <DatePicker SelectedDate="{Binding JoinedAt}"/>
        </StackPanel>

        <CheckBox Grid.Row="3" Content="Модератор" 
                  IsChecked="{Binding IsModerator}" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Click="Save_Click" />
            <Button Content="Отмена" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
